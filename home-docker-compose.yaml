services:
  ironbook_home:
    build:
      context: .
      dockerfile: apps/home/Dockerfile
    container_name: ironbook_home
    ports:
      - "${PORT:-3000}:3000"
    environment:
      NEXT_PUBLIC_SITE_URL: ${NEXT_PUBLIC_SITE_URL}
    restart: unless-stopped